//PROJETO DE ADICIONAR MAIS ELEMENTOS AO ARRAY

// function add() {
//     let productName = document.querySelector("#produtcName").value
//     let valor = document.querySelector("#valor").value
//     let ingredientes = document.querySelector("#ingredientes").value
//     let begano = document.querySelector("#begano").value

//     let destino = document.querySelector("#destinoIMG").files[0]
//     if (destino) {
//         // Crie um leitor de arquivo
//         let reader = new FileReader();

//         // Defina uma função de callback para quando o arquivo terminar de ser lido
//         reader.onloadend = function () {
//             // Aqui você pode acessar a imagem como base64 através de reader.result
//             let base64Image = reader.result;

//             // Crie o novo objeto com o caminho da imagem convertida
//             let newObject = {
//                 name: productName,
//                 price: Number(valor),
//                 descrição: ingredientes,
//                 vegan: begano,
//                 // Aqui você usa a imagem convertida
//                 src: base64Image
//             };

//             // Adicione o novo objeto ao array menuOptions
//             menuOptions.push(newObject);

//             console.log(menuOptions);
//         };

//         // Leia o arquivo de imagem como uma URL de dados (base64)
//         reader.readAsDataURL(destino);
//     } else {
//         // Caso nenhum arquivo tenha sido selecionado, adicione o objeto sem a imagem
//         let newObject = {
//             name: productName,
//             price: Number(valor),
//             descrição: ingredientes,
//             vegan: begano
//             // Não inclua a imagem aqui
//         };

//         menuOptions.push(newObject);

//         console.log(menuOptions);
//     }
//     // let newObject = {name:productName, price:Number(valor), descrição:ingredientes, vegan:begano, src:destino}
//     // menuOptions.push(newObject)
//     // console.log(menuOptions)
//     // return newObject
// }















function mudar() {
                menuOptions.map((item) => {
                    const discountedPrice = item.price - (item.price / 100) * 10;
                    item.price = discountedPrice;
                    return item;
                });
                return menuOptions
            }